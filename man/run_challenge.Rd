% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-challenge.R
\name{run_challenge}
\alias{run_challenge}
\title{Run an rgrind puzzle against your own solution}
\usage{
run_challenge(challenge_id, user_fun)
}
\arguments{
\item{challenge_id}{Character. The id of the challenge to run.
See \code{\link[=list_challenges]{list_challenges()}} for all available ids.}

\item{user_fun}{Function. Your own solution to test — written and
defined in your own R session, then passed in directly.}
}
\value{
Invisibly, a list with \code{passed} (number of test cases
passed) and \code{total} (total number of test cases).
}
\description{
Tests a user-submitted function against a challenge's test cases,
printing styled pass/fail feedback to the console. On a full pass,
shows an explanation of the idiomatic solution and updates your
local solving streak. On any failure, shows details for the failed
tests and a hint.
}
\examples{
old_opt <- options(rgrind.storage_dir = tempdir())
my_solution <- function(x) sum(x[x \%\% 2 == 0], na.rm = TRUE)
run_challenge("sum_evens", my_solution)
options(old_opt)

}
